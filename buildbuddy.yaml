actions:
  - name: "Test all targets"
    container_image: "ubuntu-20.04"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    steps:
      - run: "bazel test //..."
